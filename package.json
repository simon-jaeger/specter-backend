{
    "private": true,
    "scripts": {
        "seed": "php artisan migrate:fresh --seed",
        "clean": "php artisan clean",
        "refresh": "composer dump-autoload && php artisan optimize:clear",
        "helper": "php artisan clear-compiled && php artisan ide-helper:generate && php artisan ide-helper:models -M && php artisan ide-helper:meta"
    }
}
